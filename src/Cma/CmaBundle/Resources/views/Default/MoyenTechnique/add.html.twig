{% extends 'CmaBundle:Administrator:index.html.twig' %}


{% block body %}
    <section id="content"><main class="main"><div class="clearfix">
                <h4>
                    <i class="icon-edit"></i> Ajouter un moyens technique :</h4>
            </div>
            <div class="row-fluid">
                <div class="span7">
                    <section class="panel">

                        <form action="{{ path('image_add') }}" class="form-horizontal" method="POST"  {{ form_enctype(form) }} fole="form" >
                            <div class="row-fluid">
                                <div class="span7">
                                    {{ form_errors(form) }}
                                    <div class="control-group"> 
                                        {{ form(form) }}
                                    </div>

                                    {{ form_rest(form) }}
                                    <div class="control-group">
                                        <div class="controls">                      

                                            <button type="submit" class="btn btn-primary">Save</button>
                                            <a href="{{path('image_index')}}" slyle="margin-left:0px;"><button class="btn btn-white">Cancel</button></a>
                                            <input value="" type="hidden">
                                        </div>
                                    </div>
                                </div>

                            </div>
                        </form>
                    </section>
                {% endblock %}