{% extends 'CmaBundle:Administrator:index.html.twig' %}


{% block body %}

    <section id="content"><main class="main"><div class="clearfix">
                <h4>
                    <i class="icon-edit"></i>Modifier slide :</h4>
            </div>
            <div class="row-fluid">
                <div class="span12">
                    <section class="panel">

                        <form action="{{ path('image_edit',{'id': images.id}) }}" class="form-horizontal" method="POST"  {{ form_enctype(form) }} fole="form" >
                            <div class="row-fluid">
                                <div class="span12">
                                    {{ form_errors(form) }}
                                    {% if images.tag %}
                                        <img src="{{ asset(images.getWebPath()) }}" width="612px" tag="{{images.tag}}" />
                                    {% else %}
                                        <h2>Pas d'image de profil chargée !</2>
                                        {% endif %}
                                        <div class="control-group">

                                            {{ form(form) }}
                                        </div>
                                        {{ form_rest(form) }}
                                        <div class="control-group">
                                            <div class="controls">                      
                                                <a href="{{path('image_index')}}" slyle="margin-left:0px;"><button class="btn btn-white">Cancel</button></a>
                                                <button type="submit" class="btn btn-primary">Save</button>
                                                <input value="" type="hidden">
                                            </div>
                                        </div>
                                </div>
                            </div>
                        </form>
                    </section>

                {% endblock %}
