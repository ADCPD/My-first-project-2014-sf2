{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Social list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Reseau</th>
                <th>Url</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('social_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.reseau }}</td>
                <td>{{ entity.url }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('social_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('social_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('social_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
